
// ----- MOBILE -----

// 1.
// LingoCommand logo in top left corner with fontStretch 50%

// 2.
// 'Japanese', 'About', and 'Login' are present in navbar. None are underlined.

// 3.
// 'Contact' is not present in navbar.

// 4.
// 'Login' in navbar has normal font weight. 

// 5.
// 'Learn Japanese Faster' text is present in middle of the screen with 0.25 rem
// letter spacing

// 6.
// Blue 'Start Now' button is present in the middle of the screen below 
// 'Learn Japanese Faster' with 0.25 rem letter spacing. Also if you press 
// the button then gets redirected to '/japanese/home-page'

// 7.
// Mount Fuji picture is the background

// 8.
// If user clicks on 'LingoCommand' logo in navbar then users stays on '/'

// 9.
// If user clicks on 'Japanese' in navbar then gets redirected to '/japanese/home-page'

// 10.
// If user clicks on 'About' in navbar then gets redirected to '/japanese/about'

// 11.
// If user clicks on 'Login' in navbar then gets redirected to '/login'

// ----- DESKTOP -----

// 1.
// LingoCommand logo in top left corner with fontStretch 100%

// 2
// 'Japanese', 'About', 'Contact', and 'Login' are present in navbar.
// None are underlined.

// 3.
// 'Login' in navbar has bold font weight. 

// 4.
// 'Learn Japanese Faster' text is present in middle of the screen with 0.25 rem letter spacing

// 5.
// Blue 'Start Now' button is present in the middle of the screen below 
// 'Learn Japanese Faster' with 0.25 rem letter spacing. Also if you press 
// the button then gets redirected to '/japanese/home-page'

// 6.
// Mount Fuji picture is the background

// 7.
// If user clicks on 'LingoCommand' logo in navbar then users stays on '/'

// 8.
// If user clicks on 'Japanese' in navbar then gets redirected to '/japanese/home-page'

// 9.
// If user clicks on 'About' in navbar then gets redirected to '/japanese/about'

// 10.
// If user clicks on 'Login' in navbar then gets redirected to '/login'

